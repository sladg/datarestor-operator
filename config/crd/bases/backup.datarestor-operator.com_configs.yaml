---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: configs.backup.datarestor-operator.com
spec:
  group: backup.datarestor-operator.com
  names:
    kind: Config
    listKind: ConfigList
    plural: configs
    singular: config
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.matchedPVCsCount
      name: PVCs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Config is the Schema for the configs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Config
            properties:
              autoRestore:
                default: true
                description: Whether to enable automatic restore for new PVCs
                type: boolean
              env:
                description: Environment variables for authentication (e.g., AWS_ACCESS_KEY_ID,
                  AWS_SECRET_ACCESS_KEY)
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: Name of the environment variable. Must be a C_IDENTIFIER.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              repositories:
                description: A list of repositories to be backed up. A repository
                  is selected if it matches any of the repositories in this list.
                items:
                  properties:
                    backupSchedule:
                      default: 0 2 * * *
                      description: Cron expression for backup schedule (e.g., "0 2
                        * * *" for daily at 2 AM)
                      pattern: ^(\*|([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9])|\*\/([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9]))
                        (\*|([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9])|\*\/([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9]))
                        (\*|([1-9]|1[0-9]|2[0-9]|3[0-1])|\*\/([1-9]|1[0-9]|2[0-9]|3[0-1]))
                        (\*|([1-9]|1[0-2])|\*\/([1-9]|1[0-2])) (\*|([0-6])|\*\/([0-6]))$
                      type: string
                    env:
                      description: Environment variables for authentication (e.g.,
                        AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: Name of the environment variable. Must be
                              a C_IDENTIFIER.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                    priority:
                      description: Priority determines the order of backup targets
                        (lower number = higher priority)
                      format: int32
                      type: integer
                    selectors:
                      description: A list of selectors for PVCs to be backed up. A
                        PVC is selected if it matches any of the selectors in this
                        list.
                      items:
                        description: Selector defines how to select PVCs for backup.
                        properties:
                          autoRestore:
                            default: true
                            description: |-
                              AutoRestore specifies whether to enable automatic restore for new PVCs
                              @TODO: Use this parameter. Currently ignored, only Config-level is used.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: Label selector for PVCs.
                            type: object
                          matchNamespaces:
                            description: A list of namespaces to select PVCs from.
                              If empty, it will search in all namespaces.
                            items:
                              type: string
                            type: array
                          stopPods:
                            default: false
                            description: |-
                              StopPods specifies whether to scale down workloads using the PVCs matched by this selector.
                              @TODO: Use this parameter. Currently ignored, only Config-level is used.
                            type: boolean
                        type: object
                      type: array
                    target:
                      description: Restic repository target (e.g., s3:s3.amazonaws.com/bucket/path)
                      type: string
                  required:
                  - backupSchedule
                  - priority
                  - target
                  type: object
                type: array
              selectors:
                description: A list of selectors for PVCs to be backed up. A PVC is
                  selected if it matches any of the selectors in this list.
                items:
                  description: Selector defines how to select PVCs for backup.
                  properties:
                    autoRestore:
                      default: true
                      description: |-
                        AutoRestore specifies whether to enable automatic restore for new PVCs
                        @TODO: Use this parameter. Currently ignored, only Config-level is used.
                      type: boolean
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: Label selector for PVCs.
                      type: object
                    matchNamespaces:
                      description: A list of namespaces to select PVCs from. If empty,
                        it will search in all namespaces.
                      items:
                        type: string
                      type: array
                    stopPods:
                      default: false
                      description: |-
                        StopPods specifies whether to scale down workloads using the PVCs matched by this selector.
                        @TODO: Use this parameter. Currently ignored, only Config-level is used.
                      type: boolean
                  type: object
                type: array
              stopPods:
                default: false
                description: StopPods specifies whether to scale down workloads using
                  the PVCs matched by this selector.
                type: boolean
            required:
            - repositories
            type: object
          status:
            description: status contains the observed state
            properties:
              initializedAt:
                description: Initialized at
                format: date-time
                type: string
              matchedPVCsCount:
                description: Number of PVCs currently selected by this config
                format: int32
                type: integer
              repositories:
                description: Repositories status
                items:
                  properties:
                    backups:
                      description: Known backups
                      items:
                        type: string
                      type: array
                    initializedAt:
                      description: Time when the repository was initialized
                      format: date-time
                      type: string
                    lastScheduledBackupRun:
                      description: Information about the last scheduled backup run
                        for this target
                      format: date-time
                      type: string
                    target:
                      description: Restic repository target (e.g., s3:s3.amazonaws.com/bucket/path)
                      type: string
                  type: object
                type: array
              statistics:
                description: Statistics about the repository
                properties:
                  failedBackups:
                    description: Number of backups run failed so far
                    format: int32
                    type: integer
                  runningBackups:
                    description: Number of backups in-flight currently
                    format: int32
                    type: integer
                  successfulBackups:
                    description: Number of backups run successfully so far
                    format: int32
                    type: integer
                type: object
              workloadInfo:
                additionalProperties:
                  properties:
                    kind:
                      description: Kind of the workload
                      type: string
                    name:
                      description: Name of the workload
                      type: string
                    replicas:
                      description: Replicas of the workload
                      format: int32
                      type: integer
                  required:
                  - kind
                  - name
                  - replicas
                  type: object
                description: Workload information
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
