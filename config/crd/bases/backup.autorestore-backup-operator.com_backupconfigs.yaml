---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: backupconfigs.backup.autorestore-backup-operator.com
spec:
  group: backup.autorestore-backup-operator.com
  names:
    kind: BackupConfig
    listKind: BackupConfigList
    plural: backupconfigs
    singular: backupconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.matchingPVCs
      name: PVCs
      type: integer
    - jsonPath: .status.backupTargets
      name: Targets
      type: integer
    - jsonPath: .status.backupJobs.successful
      name: Backup Success
      type: integer
    - jsonPath: .status.backupJobs.running
      name: Backup Running
      type: integer
    - jsonPath: .status.backupJobs.failed
      name: Backup Failed
      type: integer
    - jsonPath: .status.restoreJobs.successful
      name: Restore Success
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackupConfig is the Schema for the backupconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BackupConfig
            properties:
              autoRestore:
                description: Whether to enable automatic restore for new PVCs
                type: boolean
              backupTargets:
                description: Backup targets with priorities
                items:
                  description: BackupTarget defines a backup destination with priority
                  properties:
                    name:
                      description: Name of the backup target
                      type: string
                    priority:
                      description: Priority determines the order of backup targets
                        (lower number = higher priority)
                      format: int32
                      type: integer
                    restic:
                      description: Restic configuration for backup targets
                      properties:
                        env:
                          description: Environment variables for authentication (e.g.,
                            AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: Name of the environment variable. Must
                                  be a C_IDENTIFIER.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        flags:
                          description: Additional restic flags
                          items:
                            type: string
                          type: array
                        host:
                          description: Hostname for backup identification
                          type: string
                        image:
                          description: Image for the restic job
                          type: string
                        password:
                          description: Password for restic repository
                          type: string
                        repository:
                          description: Restic repository URL (e.g., s3:s3.amazonaws.com/bucket)
                          type: string
                        tags:
                          description: Backup tags for organization
                          items:
                            type: string
                          type: array
                      required:
                      - repository
                      type: object
                    retention:
                      description: Retention policy for this target
                      properties:
                        keepDaily:
                          description: Keep the most recent snapshot for each of the
                            last n days (restic --keep-daily)
                          format: int32
                          type: integer
                        keepHourly:
                          description: Keep the most recent snapshot for each of the
                            last n hours (restic --keep-hourly)
                          format: int32
                          type: integer
                        keepLast:
                          description: Keep the last n snapshots (restic --keep-last)
                          format: int32
                          type: integer
                        keepMonthly:
                          description: Keep the most recent snapshot for each of the
                            last n months (restic --keep-monthly)
                          format: int32
                          type: integer
                        keepTags:
                          description: Keep snapshots with these tags (restic --keep-tag)
                          items:
                            type: string
                          type: array
                        keepWeekly:
                          description: Keep the most recent snapshot for each of the
                            last n weeks (restic --keep-weekly)
                          format: int32
                          type: integer
                        keepWithin:
                          description: Keep all snapshots within this duration (restic
                            --keep-within, e.g. "2y5m7d3h")
                          type: string
                        keepYearly:
                          description: Keep the most recent snapshot for each of the
                            last n years (restic --keep-yearly)
                          format: int32
                          type: integer
                        maxSnapshots:
                          description: 'Deprecated: Use KeepLast instead'
                          format: int32
                          type: integer
                        prune:
                          default: true
                          description: Whether to run prune automatically after forget
                            (restic --prune)
                          type: boolean
                        schedule:
                          default: 0 3 * * *
                          description: |-
                            Cron schedule for retention checks (e.g., "0 3 * * *" for daily at 3 AM)
                            If not specified, defaults to "0 3 * * *"
                          pattern: ^(\*|([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9])|\*\/([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9]))
                            (\*|([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9])|\*\/([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9]))
                            (\*|([1-9]|1[0-9]|2[0-9]|3[0-1])|\*\/([1-9]|1[0-9]|2[0-9]|3[0-1]))
                            (\*|([1-9]|1[0-2])|\*\/([1-9]|1[0-2])) (\*|([0-6])|\*\/([0-6]))$
                          type: string
                      type: object
                  required:
                  - name
                  - priority
                  - restic
                  type: object
                type: array
              manualBackup:
                description: Manual backup trigger. If set to true, a backup will
                  be triggered immediately and the field will be reset to false after
                  completion.
                type: boolean
              pvcSelector:
                description: PVC selector configuration
                properties:
                  labelSelector:
                    description: Label selector for PVCs
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  names:
                    description: PVC names to include
                    items:
                      type: string
                    type: array
                  namespaces:
                    description: Namespace selector
                    items:
                      type: string
                    type: array
                type: object
              schedule:
                description: Backup schedule configuration
                properties:
                  cron:
                    description: Cron expression for backup schedule (e.g., "0 2 *
                      * *" for daily at 2 AM)
                    pattern: ^(\*|([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9])|\*\/([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9]))
                      (\*|([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9])|\*\/([0-9]|1[0-9]|2[0-9]|3[0-9]|4[0-9]|5[0-9]))
                      (\*|([1-9]|1[0-9]|2[0-9]|3[0-1])|\*\/([1-9]|1[0-9]|2[0-9]|3[0-1]))
                      (\*|([1-9]|1[0-2])|\*\/([1-9]|1[0-2])) (\*|([0-6])|\*\/([0-6]))$
                    type: string
                  stopPods:
                    description: Whether to stop pods during backup for data integrity
                    type: boolean
                  waitForHealthy:
                    description: Wait for pod health before backup
                    type: boolean
                required:
                - cron
                type: object
            required:
            - backupTargets
            - pvcSelector
            - schedule
            type: object
          status:
            description: status defines the observed state of BackupConfig
            properties:
              backupJobs:
                description: Backup job statistics
                properties:
                  failed:
                    description: Number of failed jobs
                    format: int32
                    type: integer
                  pending:
                    description: Number of pending jobs
                    format: int32
                    type: integer
                  running:
                    description: Number of currently running jobs
                    format: int32
                    type: integer
                  successful:
                    description: Number of successful jobs
                    format: int32
                    type: integer
                  total:
                    description: Total number of jobs
                    format: int32
                    type: integer
                type: object
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastBackup:
                description: Last backup timestamp
                format: date-time
                type: string
              lastRetentionCheck:
                description: Last time retention policy was applied
                format: date-time
                type: string
              managedPVCs:
                description: List of PVCs being managed
                items:
                  type: string
                type: array
              nextBackup:
                description: Next backup timestamp
                format: date-time
                type: string
              restoreJobs:
                description: Restore job statistics
                properties:
                  failed:
                    description: Number of failed jobs
                    format: int32
                    type: integer
                  pending:
                    description: Number of pending jobs
                    format: int32
                    type: integer
                  running:
                    description: Number of currently running jobs
                    format: int32
                    type: integer
                  successful:
                    description: Number of successful jobs
                    format: int32
                    type: integer
                  total:
                    description: Total number of jobs
                    format: int32
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
