---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: resticbackups.backup.autorestore-backup-operator.com
spec:
  group: backup.autorestore-backup-operator.com
  names:
    kind: ResticBackup
    listKind: ResticBackupList
    plural: resticbackups
    singular: resticbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.backupName
      name: Backup Name
      type: string
    - jsonPath: .spec.pvcRef.name
      name: PVC
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.snapshotID
      name: Snapshot ID
      type: string
    - jsonPath: .status.size
      name: Size
      type: string
    - jsonPath: .spec.repositoryRef.name
      name: Repository
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResticBackup is the Schema for the resticbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResticBackupSpec defines the desired state of ResticBackup
              (merged with BackupJob)
            properties:
              backupName:
                description: Human-readable backup name
                type: string
              backupType:
                default: scheduled
                description: Backup type (scheduled, manual, auto-restore)
                type: string
              pvcRef:
                description: PVC that was/is being backed up
                properties:
                  name:
                    description: Name of the PVC
                    type: string
                  namespace:
                    description: Namespace of the PVC
                    type: string
                required:
                - name
                - namespace
                type: object
              repositoryRef:
                description: Reference to the ResticRepository where this backup is
                  stored
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              snapshotID:
                description: Restic snapshot ID (populated when backup completes successfully)
                type: string
              tags:
                description: Tags associated with this backup
                items:
                  type: string
                type: array
            required:
            - backupName
            - pvcRef
            - repositoryRef
            type: object
          status:
            description: ResticBackupStatus defines the observed state of ResticBackup
              (merged with BackupJob)
            properties:
              backupID:
                description: Backup ID assigned by the system (for tracking)
                type: string
              backupJobRef:
                description: Reference to the Kubernetes Job that executed the backup
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              backupTime:
                description: Time when the backup was created in the repository
                format: date-time
                type: string
              completionTime:
                description: Time when the backup job completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deletionJobRef:
                description: Reference to the deletion job (if any)
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              error:
                description: Error message if backup is in failed state
                type: string
              lastVerified:
                description: Last time this backup's existence was verified in the
                  repository
                format: date-time
                type: string
              phase:
                description: Current phase of the backup (Pending, Running, Completed,
                  Ready, Failed)
                enum:
                - Pending
                - Running
                - Completed
                - Ready
                - Failed
                type: string
              size:
                description: Size of the backup in bytes
                format: int64
                type: integer
              startTime:
                description: Time when the backup job started
                format: date-time
                type: string
              verificationJobRef:
                description: Reference to the verification job (if any)
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
