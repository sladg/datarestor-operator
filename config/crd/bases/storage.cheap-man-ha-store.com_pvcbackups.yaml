---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: pvcbackups.storage.cheap-man-ha-store.com
spec:
  group: storage.cheap-man-ha-store.com
  names:
    kind: PVCBackup
    listKind: PVCBackupList
    plural: pvcbackups
    singular: pvcbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.backupStatus
      name: Status
      type: string
    - jsonPath: .status.lastBackup
      name: Last Backup
      type: date
    - jsonPath: .status.managedPVCs
      name: PVCs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PVCBackup is the Schema for the pvcbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PVCBackup
            properties:
              autoRestore:
                description: Whether to enable automatic restore for new PVCs
                type: boolean
              backupTargets:
                description: Backup targets with priorities
                items:
                  description: BackupTarget defines a backup destination with priority
                  properties:
                    name:
                      description: Name of the backup target
                      type: string
                    nfs:
                      description: NFS configuration for NFS-type targets
                      properties:
                        mountOptions:
                          description: NFS mount options
                          items:
                            type: string
                          type: array
                        path:
                          description: NFS export path
                          type: string
                        server:
                          description: NFS server address
                          type: string
                      required:
                      - path
                      - server
                      type: object
                    priority:
                      description: Priority determines the order of backup targets
                        (lower number = higher priority)
                      format: int32
                      type: integer
                    retention:
                      description: Retention policy for this target
                      properties:
                        maxAge:
                          description: Maximum age of snapshots to keep
                          type: string
                        maxSnapshots:
                          description: Maximum number of snapshots to keep
                          format: int32
                          type: integer
                      required:
                      - maxSnapshots
                      type: object
                    s3:
                      description: S3 configuration for S3-type targets
                      properties:
                        accessKeyID:
                          description: S3 access key ID
                          type: string
                        bucket:
                          description: S3 bucket name
                          type: string
                        endpoint:
                          description: S3 endpoint (for custom S3-compatible storage)
                          type: string
                        pathPrefix:
                          description: S3 path prefix
                          type: string
                        region:
                          description: S3 region
                          type: string
                        secretAccessKey:
                          description: S3 secret access key
                          type: string
                      required:
                      - accessKeyID
                      - bucket
                      - region
                      - secretAccessKey
                      type: object
                    type:
                      description: Type of backup target (s3, nfs, etc.)
                      type: string
                  required:
                  - name
                  - priority
                  - type
                  type: object
                type: array
              initContainer:
                description: Init container configuration for restore waiting
                properties:
                  args:
                    description: Args for the init container
                    items:
                      type: string
                    type: array
                  command:
                    description: Command for the init container
                    items:
                      type: string
                    type: array
                  image:
                    description: Image for the init container
                    type: string
                required:
                - image
                type: object
              pvcSelector:
                description: PVC selector configuration
                properties:
                  labelSelector:
                    description: Label selector for PVCs
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  names:
                    description: PVC names to include
                    items:
                      type: string
                    type: array
                  namespaces:
                    description: Namespace selector
                    items:
                      type: string
                    type: array
                type: object
              restic:
                description: Restic configuration for plain data backup
                properties:
                  flags:
                    description: Additional restic flags
                    items:
                      type: string
                    type: array
                  password:
                    description: Restic password
                    type: string
                  repository:
                    description: Restic repository
                    type: string
                required:
                - password
                - repository
                type: object
              schedule:
                description: Backup schedule configuration
                properties:
                  cron:
                    description: Cron expression for backup schedule
                    type: string
                  stopPods:
                    description: Whether to stop pods during backup for data integrity
                    type: boolean
                  waitForHealthy:
                    description: Wait for pod health before backup
                    type: boolean
                required:
                - cron
                type: object
            required:
            - backupTargets
            - pvcSelector
            - schedule
            type: object
          status:
            description: status defines the observed state of PVCBackup
            properties:
              backupStatus:
                description: Current backup status
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedBackups:
                description: Number of failed backups
                format: int32
                type: integer
              lastBackup:
                description: Last backup timestamp
                format: date-time
                type: string
              lastRestore:
                description: Last restore timestamp
                format: date-time
                type: string
              managedPVCs:
                description: List of PVCs being managed
                items:
                  type: string
                type: array
              restoreStatus:
                description: Current restore status
                type: string
              successfulBackups:
                description: Number of successful backups
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
