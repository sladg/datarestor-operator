apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "datarestor-operator.fullname" . }}-config-admin-role
  labels:
  {{- include "datarestor-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - backup.datarestor-operator.com
  resources:
  - configs
  verbs:
  - '*'
- apiGroups:
  - backup.datarestor-operator.com
  resources:
  - configs/status
  verbs:
  - get